import java.util.List;

public class Splitter {

    public enum Encoding {
        GSM,
        Unicode
    }

    public static List<String> splitIntoParts(String message, Encoding encoding) {
        return null;
    }
}
